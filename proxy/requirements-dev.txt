# proxy/requirements-dev.txt — exact pinned dev/test dependencies
# Derived from proxy/pyproject.toml [project.optional-dependencies.dev]
# Pin strategy: exact versions (==) matching pyproject.toml; no ranges.
# To update: uv pip compile pyproject.toml --extra dev -o requirements-dev.txt
# Last updated: 2026-06-04

pytest==8.3.5
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.28.1
ruff==0.15.22
mypy==1.15.0
factory-boy==3.3.3
freezegun==1.5.1
