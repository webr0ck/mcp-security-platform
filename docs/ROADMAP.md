# ROADMAP — server management & onboarding work (living doc)

> **Clean-session handoff.** This file is the single place to resume this stream
> of work without prior chat context. It is kept current as work lands — if you
> change something here, update this file in the same commit. It tracks the
> *current initiative* (server management + onboarding lifecycle redesign); the
> product-wide control status lives in `README.md`'s Enforced-vs-Roadmap table.
>
> Last updated: 2026-07-19 · Branch: `main`

## How to use this in a fresh session
1. Read this file top to bottom, then the referenced PRD/specs.
2. "In progress" tells you exactly what's mid-flight and how to continue.
3. To verify the lab end-to-end: `make -f Makefile.lab lab-reset` (full wipe) →
   `lab-smoke` → `make test-lab-functional`. `.env.lab` must have `OPS_AGENT_TOKEN`
   + `PODMAN_SOCK=/run/user/501/podman/podman.sock` (auto-generated by
   `lab-setup.sh` on a fresh clone).

---

## ✅ Shipped (committed to `main`, fresh-boot verified 2026-07-19)

Verified on a real `lab-reset` wipe: `lab-smoke` 4/4, `test-lab-functional`
46 passed / 1 skipped / 0 failed.

### Acceptance-run fix batch (external report 2026-07-18_21_40)
| Fix | What | Commit |
|---|---|---|
| 1 | Named-profile **default-deny** once a profile has any binding (INV-016) | `d657472` |
| 2 | Entitlement `principal_id` **shape validation** (reject bare-username) | `005c22a` |
| 3 | `search-kb` **wrapper-name mismatch** in invoke_tool (misrouted, not phantom) | `02e9b18` |
| 4 | `data_categories` unknown-category is **non-name-consuming** (`extra="forbid"`) | `005c22a` |
| 5 | Profile-naming **clarity docs** + V081 tool-description tweak | `391e1ee`,`9ea4eff` |
| 6 | Credential-upload UI (already wired) + docs cross-link | `9ea4eff` |
| 7 | Audit **taint notice** moved out of `deny_reasons` → `notices` field | `d657472` |
| 8 | Compose **port-exposure** leak closed (OPA :8181, backends :8100-8113) + isolation gate (INV-017) | `d7cfd11` |

### Server management feature (WS-A) + hardening
- **ops-agent** (isolated socket-holder) + proxy `admin_ops.py` (logs/restart/rebuild) — `04a582f`
- **IDOR hardening**: restart/rebuild platform_admin-only, container allowlist, audit — `326f7e6`
- Admin server list serves `debug_mode`/`trust_tier` — `d0a9798`
- ops-agent **actually works**: podman client + `CONTAINER_HOST` in image — `5551b75`
- ops-agent must **not gate proxy boot** + correct socket path (`/run/user/501`) — `f954c30`
- `OPS_AGENT_TOKEN` auto-gen in `lab-setup.sh` (fresh-clone boots) — `45c8ac5`
- logs endpoint honestly **admin-only** (matches `/api/v1/admin` RBAC prefix) — `9b43c15`
- **View logs** button on the **portal** admin server card (the UI actually in use) — `e0e1bd0`

### Codex OAuth (earlier in session)
- RFC 9207 issuer consistency + stop advertising `iss` param (Codex logs in, no bearer/downgrade) — `316e105`,`e23d62c`

---

## ✅ "Finish all" batch — DONE + FRESH-BOOT VERIFIED (2026-07-19)
Fresh `lab-reset` (V083 + V082 apply clean, ops-agent rebuilt with `git`, new
egress/workdir wiring, Phase 2 UI, convergence refactor all boot healthy):
**lab-smoke 4/4 · test-lab-functional 46/1/0 · F-001 isolation gate ALL PASS**
(incl. new POC-tier fix); git-rebuild endpoint wired + fail-closed (403 non-admin).
**Ready for owner's full check.** Everything below is on `main`.

### PRD-0012 — URL-first onboarding, re-approval on change, debug-mode-first ✅ (Phase 1 + 2)
Spec: `docs/prd/PRD-0012-...md` (v2, 3-critic-hardened). Owner decisions: self-hosted
flow is default (platform-deployed preserved via `self_host=false` submit intent);
IP-only change (same tool schema) → auto-approve, code change → full re-scan+review.
- **Phase 1 backend** (`ca0ab80`): V082 (`is_self_hosted` + `last_good_*`),
  `server_lifecycle.py` (C2 approve→verify→debug-mode, C3 `request-change`
  quarantine-all + real-`status` demote + CAS + IP-only/code split), C4 `verify`,
  PATCH reroute, reject-rolls-back-to-last-good, guarded `change_rereview_scan`.
  **6 critic traps closed; appsec audit 8/9 hold (no fail-open, no bypass)**;
  fresh-boot verified once already (smoke 4/4, functional 46/1/0, F-001 all pass).
- **Phase 2 UI** (`e0e1bd0`-era + Phase-2 commit): portal maintenance banner +
  staleness hint + View-logs/Retry-verify/Go-live; ⋯ Edit-endpoint / Update-from-git;
  submitter self-host-vs-platform-deploy choice; `changes_requested` URL field;
  React reviewer card shows the real URL.

### Also just landed (committed)
- **git-pull rebuild** (`7458a98` + convergence refactor): ops-agent
  `/ops/rebuild-from-git` clones/pulls a server's public repo + rebuilds; admin_ops
  rebuild → the ONE canonical `request_change_for_server` re-review
  (`require_self_hosted=False` — duplicate deleted, single source of truth).
- **3 backlog items** (`717d989`, `439e4bb`, `a614978`): audit `notices` persisted
  to `audit_events` (V083); `conftest.py REDIS_HOST` in-container fixed; POC-tier
  `mcp-servers-net` isolation leak closed.
- **F-001 real leaks** (`f070803`): scanner-worker→proxy reachability removed;
  per-server egress nets. Live-verified on the prior fresh boot.

---

## 🗄️ Deferred / backlog (named, not lost)
- ~~Ops-agent rebuild → true `git pull`~~ → IN PROGRESS (`bl-gitrebuild`).
- ~~`notices` field into `audit_events` table~~ → ✅ DONE (`717d989`, `bl-cleanup`).
- ~~Stale `test_taint_floor_invoke.py`~~ → ✅ DONE (`d6424f0`).
- ~~Pre-existing isolation-gate drift~~ → ✅ DONE (`f070803`).
- ~~`compose.poc.yml` `mcp-servers-net` >1 MCP server~~ → ✅ DONE (`a614978`, `bl-cleanup`).
- ~~`tests/conftest.py` `REDIS_HOST` in-container~~ → ✅ DONE (`439e4bb`, `bl-cleanup`).
- ~~Debug-mode staleness hint~~ → folded into `p2-ui` (portal maintenance banner).
- **D3 dual-control direct-registration path** (`POST /api/v1/servers`) is
  explicitly OUT of scope for PRD-0012 C1-C4 (keeps its own logic).
- **React `SubmissionReview` SBOM parity** — the portal reviewer card shows
  SBOM/scan detail; the React one doesn't (the `GET /admin/submissions` API
  doesn't return SBOM fields). Secondary path; needs an API field to bind.
- **git-rebuild image-tag assumption** — ops-agent `/ops/rebuild-from-git` tags
  `<service>:lab` and `up -d`; a service with a differently-tagged image would
  build but keep running the old image. Needs compose-YAML parse or a stored
  image_tag to close (documented in the endpoint docstring).
- **Pre-existing repo-wide ruff debt** (`make lint` red on ~1533 findings across
  untouched files) — not this work's regression; separate cleanup if wanted.

---

## 🔑 Key decisions log
- Broker KEK → persistent Vault, never plaintext `.env.lab` (3-critic rejected).
- Codex OAuth fixed server-side (no bearer token, no client downgrade).
- ops-agent is the ONLY container-socket holder; proxy never touches it;
  restart/rebuild admin-only; logs admin + `debug_mode`.
- PRD-0012: enforce on real columns at change time; reject rolls back to
  last-good; self-hosted has an accepted same-URL-swap residual risk.

## ⚠️ Gotchas for a fresh session
- `podman restart mcp-proxy` after editing `proxy/app/**` (uvicorn --reload
  doesn't detect bind-mount changes on macOS podman machine).
- Never casually `down -v`/wipe postgres outside an approved verification gate.
- `tool_registry` hard-DELETE is blocked by the audit immutability guard →
  soft-delete (`deleted_at`, `status='disabled'`), unique key `(name, version)`.
- Editing `authz.rego` is a no-op until `make sign-policy-bundle`.
