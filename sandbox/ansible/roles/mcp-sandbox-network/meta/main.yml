---
galaxy_info:
  role_name: mcp-sandbox-network
  author: mcp-security-platform
  description: Create and verify the Podman internal-only sandbox network
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions: ["22.04", "24.04"]
    - name: Fedora
      versions: [all]
    - name: EL
      versions: ["8", "9"]
    - name: MacOSX
      versions: [all]
dependencies:
  - role: mcp-sandbox-prereqs
