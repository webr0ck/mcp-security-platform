*.pyc
__pycache__/
# Generated per-run acceptance test artifacts (results) and regenerated-fresh-
# each-run TLS fixtures (certs, keys) — never source, never commit.
lab/tests/acceptance/results/
lab/tests/acceptance/fixtures/certs/
lab/tests/acceptance/fixtures/certs_for_proxy/
.env
.env.lab
*.egg-info/
.venv/
dist/
.mypy_cache/
.pytest_cache/
.ruff_cache/
# F-002: signed OPA bundle is a build artifact (signed with POLICY_SIGNING_KEY).
# Never commit it — rebuild via `make sign-policy-bundle`.
policies/bundle.tar.gz
# local pre-change safety copy (F-001 network surgery)
docker-compose.yml.pre-f001-bak
server.crt
server.key
lab/nginx/lab-certs/*.pem
# RT-004 fix: local key override written after red-team rotation — never commit
proxy/.env
# Fix 8 (docs/spec/11-server-lifecycle-and-hardening-batch.md): opt-in dev
# convenience overlay that restores host-port debugging access. Never
# committed — see docker-compose.override.yml.example for the template.
docker-compose.override.yml
# OPA-002/003: bearer token is a runtime secret — never commit
infra/secrets/*
!infra/secrets/.gitkeep
# Vendored / generated / local-state — never commit
node_modules/
.claude/
.understand-anything/
out/
lab/logs/
# lab secrets (step-ca CA password etc.) — never commit
lab/step-ca-password

# Large stress-test artifact (regeneratable) — keep out of git
lab/stress/results.json

# Build / scan / evidence artifacts (regeneratable) — never commit
dep-audit-report.json
runtime-evidence.json
graphify-out/
.understand-anything/
out/

# Local editor / pre-change backups — never commit
*.bak

# Lab TLS material (regenerated by step-ca / make targets) — never commit
lab/nginx/lab-certs/*.crt
lab/nginx/lab-certs/*.key

# Personal agent guidance + internal AI planning docs — not for public repo
CLAUDE.md
docs/superpowers/
lab.config

# PRD-0006 R-2: generated lab gateway secret + step-ca certs (never commit)
lab/nginx/secrets/
lab/nginx/lab-certs/step-ca-root.crt

# playwright session screenshots (keep only the 2 originally tracked)
.playwright-mcp/page-*
.playwright-mcp/console-2026-07-05*
.playwright-mcp/

# WIP UI artifacts — must not go public (failing-test PNGs, unpolished design sub-project)
ui/test-results/
ui/design-proposals/

# Generated / tool-scratch output — regenerable, never committed
graphify-out/
.understand-anything/
.ops-agent-git-workdirs/
.superpowers/
dep-audit-report.json
