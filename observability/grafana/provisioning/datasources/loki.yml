apiVersion: 1

datasources:
  - name: Loki
    type: loki
    # uid must match the datasourceUid in alerting/mcp-alerts.yml rule definitions
    uid: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
    jsonData:
      maxLines: 5000
      timeout: 60
