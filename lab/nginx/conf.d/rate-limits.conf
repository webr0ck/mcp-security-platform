# Lab: rate limiting zones — mirrors gateway/nginx/nginx.conf http{} definitions.
# Placed in conf.d because step 20 overwrites nginx.conf with the CRS template.
limit_req_zone $binary_remote_addr zone=api:10m rate=30r/m;
# zone=agent uses $ssl_client_s_dn_cn which requires mTLS — not defined in lab
