{
  "name": "mcp-security-platform-acceptance",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Playwright acceptance suite for the server-rendered portal (proxy/app/routers/portal.py). The React SPA that used to live in ui/src was deleted 2026-07-25 — it was never deployed: no compose service, no nginx location, and no CI job built it.",
  "scripts": {
    "acceptance": "playwright test --config playwright.portal.config.ts"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.0"
  }
}
