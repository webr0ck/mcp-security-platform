---
# Fixture: lab-mcp-bad has REDIS_PASSWORD in env — VIOLATES rule (iii).

services:
  lab-mcp-bad:
    image: mcp:test
    networks:
      - lab-net
    environment:
      HOST: "0.0.0.0"
      REDIS_PASSWORD: "supersecret"    # VIOLATION: platform credential in env

networks:
  lab-net:
    driver: bridge
